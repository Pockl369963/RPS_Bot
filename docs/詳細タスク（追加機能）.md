# 詳細タスクリスト（追加機能）：RNN Predictor

`詳細設計.md` に追加されたRNN予測機能を実装するためのタスクリストです。

## Phase 7: RNN Predictorの実装 (Machine Learning Integration)

ディープラーニングモデル(LSTM)を予測器として組み込みます。

- [x] **Git: ブランチ作成**
    - [x] `git checkout -b feature/rnn-predictor`
- [x] **7.1 環境構築**
    - [x] `pyproject.toml` に `torch`, `numpy` を追加
    - [x] `uv sync` でインストール
- [x] **7.2 LSTMモデルの実装**
    - [x] `src/game/ai/models.py` (新規) を作成
    - [x] `RPSLSTM` クラス (extends `nn.Module`) を定義
        - [x] Input: (Batch, Seq, Features)
        - [x] Hidden Layer: LSTM cell
        - [x] Output: (Batch, 3) -> Softmax
- [x] **7.3 RNN予測器の実装 (TDD)**
    - [x] `tests/test_rnn.py` を作成 (Red)
    - [x] `src/game/ai/predictors.py` に `RNNPredictor` を追加 (Green)
        - [x] データ変換ロジック (History -> Tensor)
        - [x] `train()` メソッド: 直近のデータで1ステップ学習
        - [x] `predict()` メソッド: ある程度の履歴が溜まったら発動
- [x] **7.4 メタ戦略への統合**
    - [x] `src/game/ai/strategy.py` の `StrategySelector` にRNNを追加
    - [x] 統合テストを実施
- [ ] **Git: マージ & プッシュ**
    - [ ] `git add .` -> `git commit -m "Implement RNN predictor"`
    - [ ] `git checkout main`
    - [ ] `git merge feature/rnn-predictor`
    - [ ] `git push origin main`
